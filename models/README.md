# Models

The selected model is a MobileNetV2 classifier with ImageNet initialization and a frozen convolutional base, trained with moderated class weighting.

The original `.keras` checkpoint was retained in the private research archive and is not included in this GitHub package.
